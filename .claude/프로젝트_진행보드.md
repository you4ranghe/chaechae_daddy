# chaechae-daddy 프로젝트 진행보드
 cd chaechae-daddy; npm run dev


## Phase 2 MVP (완료)
- [x] Supabase 인증 (로그인/회원가입)
- [x] 랜딩페이지
- [x] 프라이싱 페이지
- [x] 협찬 DM 분석 에이전트 (orchestrator + sponsorship-agent)
- [x] 콘텐츠 생성 에이전트 (content-planner + SSE 스트리밍)
- [x] 대시보드 UI (통계 카드, 협찬 관리, 사용량)
- [x] 플랜별 사용량 제한 시스템

---

## Phase 2.1 — 대시보드 실데이터 + 프로필 설정
- [x] 대시보드 통계 실데이터 연동 (목업 → Supabase 쿼리)
- [x] Supabase 마이그레이션 DDL 작성 (profiles에 instagram_handle, follower_count, categories 추가)
- [x] 프로필/설정 페이지 (/dashboard/settings) + API (/api/profile PATCH)
- [x] 사이드바에 설정 메뉴 추가
- [x] 에이전트 API에서 profiles 테이블 컨텍스트 조회로 전환

## Phase 2.2 — Phase 1 에이전트 포팅
- [x] analytics-agent 포팅 (주간 성과 리포트)
- [x] hashtag-agent 포팅 (해시태그 전략 분석)
- [x] weekly-planner 포팅 (주간 콘텐츠 캘린더, Tool Use 패턴)
- [x] Tool 파일 포팅 (trending-topics, trending-analyzer, sponsorship-db)
- [x] API 라우트 추가 (/api/agent/analytics, /api/agent/hashtag, /api/agent/content-plan)
- [x] UI 페이지 + 컴포넌트 추가 (/dashboard/analytics, /dashboard/hashtags, /dashboard/content-plan)
- [x] 사이드바 메뉴 추가 (성과 분석, 해시태그, 콘텐츠 플래너)

## Phase 2.3 — 결제 연동 + 온보딩
- [x] 온보딩 플로우 (회원가입 후 인스타 핸들/팔로워/카테고리 입력)
- [x] 결제 연동 (Stripe)
- [x] 이메일 알림 (트라이얼 만료, 분석 완료)
  - Resend REST API 연동 (`src/lib/email/send.ts`)
  - 트라이얼 3일/1일 전 cron 알림 (`/api/cron/trial-reminder`, vercel.json schedule)
  - 분석 완료 알림 (sponsorship/analytics/hashtag/content-plan 라우트에 fire-and-forget)
  - 설정 페이지에 알림 on/off 토글
  - 필요 환경변수: `RESEND_API_KEY`, `EMAIL_FROM`, `CRON_SECRET`, `NEXT_PUBLIC_APP_URL`
  - 마이그레이션: `supabase/migrations/20260512_add_email_notifications.sql` 실행 필요

## Phase 2.4 — 협찬 히스토리 + 인사이트
- [x] 월별 협찬 수익 그래프 (`/dashboard/insights` 최근 6개월 막대 그래프)
- [x] 브랜드별 통계 / 수락·거절 비율 (도넛 차트 + 상위 8개 브랜드 리스트, 반복 협찬 뱃지)
- [x] 중복 브랜드 감지 (새 분석 응답에 `duplicateBrand` 포함 → 분석 결과 상단 배너)
- [x] 시세 비교 시각화 (업종별 min-median-max range bar)
- [x] 협찬 상태 플로우 관리 (`PATCH /api/sponsorship/[id]`, 진행/완료 탭에 상태 전환 버튼, 완료 시 금액 입력 모달)
- 신규 파일: `src/lib/db/insights.ts`, `src/components/insights/*`, `src/components/sponsorship/status-actions.tsx`, `src/app/api/sponsorship/[id]/route.ts`, `src/app/dashboard/insights/page.tsx`

## Phase 3 — 고급 기능
- [x] 인스타 API 연동 (인사이트 자동 수집)
  - `src/lib/instagram/client.ts` (Graph API v21 클라이언트)
  - OAuth: `/api/instagram/connect` + `/api/instagram/callback` (state CSRF + long-lived 토큰)
  - 동기화: `/api/instagram/sync` POST/DELETE → `instagram_insights` 스냅샷 + profiles.follower_count 갱신
  - 설정 페이지 `InstagramConnectionCard` 연결/해제/지금 동기화 버튼
- [x] DM 자동 수집 (웹훅)
  - `/api/webhook/instagram` GET(verify) + POST(events), HMAC-SHA256 서명 검증
  - 협찬 키워드 휴리스틱 (`looksLikeSponsorship`) → 자동 sponsorship 분석 + 이메일 알림
  - `processed_dms` dedupe 테이블
- [x] 팀 기능 (매니저 → 다수 인플루언서)
  - `teams`/`team_members`/`team_invitations` 테이블 + RLS
  - API: `/api/team`, `/api/team/invitations`, `/api/team/invitations/[id]`, `/api/team/[id]/{members,pending}`
  - UI: `/dashboard/team` + CreateTeamForm/TeamPanel/InvitationsList, 사이드바 메뉴 추가
- [x] AI 피드백 루프 (포스팅 성과 → 다음 콘텐츠 반영)
  - `post_performance` 테이블 + `/api/performance` POST
  - `fetchPerformanceContext` → 상위 3 / 하위 2 인게이지먼트 발췌 → content-planner 프롬프트 주입
  - 완료 탭에 `PerformanceForm` (좋아요/댓글/도달/메모 입력)
- 필요 환경변수: META_APP_ID, META_APP_SECRET, META_REDIRECT_URI, META_WEBHOOK_VERIFY_TOKEN
- 마이그레이션: `supabase/migrations/20260512_phase3_schema.sql`
